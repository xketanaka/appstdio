#!/usr/bin/env bash
# 所有者ロールで bin/rails を実行する。スキーマ変更を伴う操作に使う。
#
#   bin/rails-as-owner db:migrate
set -e
exec env \
  DB_USER="${DB_OWNER_USER:?DB_OWNER_USER が設定されていません}" \
  DB_PASSWORD="${DB_OWNER_PASSWORD}" \
  "$(dirname "$0")/rails" "$@"
